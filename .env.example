NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional — marketplace still runs in browser demo mode without these
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional — Google Calendar OAuth (Calendar API + OAuth consent)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Shared marketplace (tasks + messages across devices)
GITHUB_TOKEN=
MARKET_GIST_ID=2cf8119bc84941220661e4544e1018c4

# Gemini — in-app support / task-matching chat (never commit the real key)
GEMINI_API_KEY=
